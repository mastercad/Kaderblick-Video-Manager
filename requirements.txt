# Kaderblick Video Manager - Dependencies
# Python 3.11+ erforderlich

# GUI
PySide6>=6.5.0

# YouTube Upload
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0

# Raspberry Pi Video-Download
# paramiko wird für Verzeichnislisting und Metadaten-Abfragen genutzt.
# Der eigentliche Dateitransfer läuft bevorzugt über rsync (System-Paket).
# Systemabhängigkeiten (optional): rsync, sshpass (sudo apt install rsync sshpass)
paramiko>=2.11.0
